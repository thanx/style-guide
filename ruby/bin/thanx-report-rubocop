#!/usr/bin/env ruby

require 'bundler'
Bundler.require

gempath = Bundler.load.specs['thanx-style'].first.full_gem_path
dangerfile = "#{gempath}/.danger/rubocop"

system("bundle exec danger --dangerfile=#{dangerfile}")
